# Fixed remote location checked by the installed app on startup.
# The manifest and questions.json should be hosted together.
# Example only; replace with the final production endpoint before building.
VITE_DATASET_MANIFEST_URL=https://data.example.com/lythuyetlaixe/dataset-manifest.json
